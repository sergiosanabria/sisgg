{% extends app.request.isXmlHttpRequest ? "::ajax.html.twig" : "SisGGFinalBundle:Cajero:cajero.html.twig" %}
{%block ruta%}
<li>
    <a href="#">Inicio</a> <span class="divider">/</span>
</li>
<li>
    <a href="#">Localidades</a> <span class="divider">/</span>
</li>
<li>
    <a href="#">Nueva Localidad</a>
</li>
{%endblock%}
{% block content %}
<div class="row-fluid sortable">
    <div class="box span12">
        <div class="box-content">
            <legend>Nueva Localidad</legend>
            <form class="form-horizontal" action="{{ path('nueva_localidad') }}" method="post">
                <fieldset id="formNuevaLocalidad">
                    <div class="span6">
                        {{form_widget(form)}}     
                </fieldset>
                <div class="form-actions">
                    <button type="submit" class="btn btn-primary">Guardar Cambios</button>
                    <button id="cancelCliente"  onclick="cancelarProvincia()" type="button"class="btn">Cancelar</button>
                </div>    
            </form>
        </div>
    </div><!--/span-->
</div><!--/row-->
<hr>
{% endblock %}
{%block javascripts%}
{{parent()}}
{%endblock%}
{% block ajax %}
{% endblock %}

