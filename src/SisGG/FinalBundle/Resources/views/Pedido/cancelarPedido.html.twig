    <div class="modal-header">
        <button type="button" class="close" data-dismiss="modal">×</button>
        <h3>Cancelar Pedido</h3>
    </div>
    <div id="dataCancelar" class="modal-body">
        ¿Esta seguro que desea cancelar el pedido para {%if pedido.isMostrador or pedido.isDelivery%}{{pedido.Solicitante}}{%else%} la Mesa {{pedido.mesa.numero}} del sector {{pedido.mesa.sector}}{%endif%}?
    </div>
    <div class="modal-footer">
        <a href="#" class="btn" data-dismiss="modal">No</a>
        <a href="#" class="btn btn-primary" id="cancelarPedido" >Si</a>
    </div>

